# 任务目标：
分析提供的测试过程中接口请求信息及响应信息，根据断言检查要求，判断接口测试是否通过
{{hint}}

# 接口请求信息
## 请求方法
> {{ response_info.request.method }}

## 请求地址
> {{ response_info.request.url }}

## 请求URL参数
> {{ response_info.request.params }}

## 请求头
```
{{ response_info.request.headers }}
```

## 请求体
```
{{ response_info.request.body }}

```

## 请求Cookie
```
{{ response_info.request.cookies }}
```


# 接口响应信息
## 响应状态码
> {{ response_info.status_code }}

## 响应头
> {{ response_info.headers }}

## 响应内容体
```

{{ response_info.text }}

```

# 断言检查规则
> 无视字母的大小写
> {{ expected }}


# 输出要求

回答内容只需要纯json字符串文本内容，json格式与回答格式符合即可。不需要添加其他无关的内容。

## 输出格式示例：用户指定的属性，如果没有就删除，有才需要加上并显示
```json
{
    "success": true,
    "message": "简述你的推理方式",
    "用户指定的属性": "属性值"
}
``` 